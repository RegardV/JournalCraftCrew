import React, { useState, useEffect } from 'react';
import {
  Plus,
  BookOpen,
  BarChart3,
  Users,
  Clock,
  Sparkles,
  FileText,
  Image,
  Settings,
  User,
  TrendingUp,
  Zap,
  Activity,
  ArrowRight
} from 'lucide-react';
import AccountSettings from '@/components/settings/AccountSettings';
import { systemAPI, projectAPI } from '@/lib/api';

interface DashboardProps {
  user?: {
    name: string;
    email: string;
  };
}

const Dashboard: React.FC<DashboardProps> = ({ user }) => {
  const [activeView, setActiveView] = useState<'dashboard' | 'settings'>('dashboard');
  const [recentProjects, setRecentProjects] = useState<Array<{
    id: string;
    title: string;
    description: string;
    status: string;
    progress: number;
    lastEdit: string;
    wordCount: string;
    filePath?: string;
    files?: Array<{ name: string; type: string; size: number }>;
  }>>([]);

  useEffect(() => {
    // Fetch LLM projects when component mounts
    const fetchLLMProjects = async () => {
      try {
        const response = await projectAPI.getLLMProjects();
        if (response.projects && response.projects.length > 0) {
          const formattedProjects = response.projects.map((project: any) => ({
            id: project.id,
            title: project.title,
            description: project.description,
            status: project.status,
            progress: project.progress || 100,
            lastEdit: new Date(project.created_at).toLocaleDateString(),
            wordCount: project.word_count || 'N/A',
            files: project.files || []
          }));
          setRecentProjects(formattedProjects);
        }
      } catch (error) {
        console.error('Error fetching LLM projects:', error);
      }
    };

    if (activeView === 'dashboard') {
      fetchLLMProjects();
    }
  }, [activeView]);

  const handleCreateJournal = () => {
    // For now, navigate to a placeholder or show a modal
    // In the future, this would open the journal creation flow
    alert('Journal creation flow coming soon! This will open the AI journal generation interface.');
  };

  const stats = [
    {
      title: 'Total Journals',
      value: '0',
      change: 'Ready to create',
      icon: BookOpen,
      color: 'from-blue-500 to-blue-600',
      trend: 'neutral'
    },
    {
      title: 'Words Written',
      value: '0',
      change: 'Start writing',
      icon: FileText,
      color: 'from-indigo-500 to-indigo-600',
      trend: 'neutral'
    },
    {
      title: 'Active Projects',
      value: '0',
      change: 'Create your first',
      icon: Users,
      color: 'from-green-500 to-green-600',
      trend: 'neutral'
    }
  ];

  
  if (activeView === 'settings') {
    return (
      <div className="space-y-6">
        <div className="flex items-center justify-between">
          <div>
            <h1 className="text-heading">Settings</h1>
            <p className="text-gray-600">Manage your account and preferences</p>
          </div>
          <button
            onClick={() => setActiveView('dashboard')}
            className="btn btn-outline"
          >
            Back to Dashboard
          </button>
        </div>
        <AccountSettings />
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-indigo-50 via-white to-purple-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 md:px-8 lg:px-12 py-6 sm:py-8 md:py-10 lg:py-12">
        <div className="space-y-4 sm:space-y-6">
          {/* Header */}
          <div className="lg:mx-10">
            <div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between">
            <div className="text-center lg:text-left">
              <h1 className="text-2xl sm:text-3xl md:text-4xl lg:text-5xl xl:text-6xl font-bold gradient-text mb-2 sm:mb-4 break-words">
                Welcome back, {user?.name}!
              </h1>
              <p className="text-base sm:text-lg text-gray-600 max-w-none sm:max-w-2xl">
                Ready to create your next AI-powered journal?
              </p>
            </div>
            <div className="flex flex-col gap-2 sm:flex-row lg:shrink-0 w-full sm:w-auto">
              <button
                onClick={() => handleCreateJournal()}
                className="btn btn-primary flex items-center justify-center gap-2 text-base sm:text-lg px-4 sm:px-6 py-3 w-full sm:w-auto shadow-lg hover:shadow-xl hover-lift"
              >
                <Plus className="w-4 h-4 sm:w-5 h-5" />
                Create New Journal
              </button>
              <button
                onClick={() => setActiveView('settings')}
                className="btn btn-outline flex items-center justify-center gap-2 px-4 sm:px-6 py-3 w-full sm:w-auto"
              >
                <Settings className="w-4 h-4 sm:w-5 h-5" />
                Settings
              </button>
            </div>
          </div>
          </div>

          {/* Stats Grid */}
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6">
        {stats.map((stat, index) => {
          const Icon = stat.icon;
          return (
            <div key={index} className="metric-card hover-lift group">
              <div className="flex items-start justify-between">
                <div className="flex-1 min-w-0">
                  <p className="text-sm text-gray-600 mb-2 font-medium truncate">{stat.title}</p>
                  <p className="text-2xl sm:text-3xl font-bold text-gray-900 mb-1">{stat.value}</p>
                  <p className={`text-sm ${
                    stat.trend === 'up' ? 'text-green-600' :
                    stat.trend === 'down' ? 'text-red-600' : 'text-gray-600'
                  }`}>
                    {stat.change}
                  </p>
                </div>
                <div className={`w-12 h-12 sm:w-14 sm:h-14 bg-gradient-to-br ${stat.color} rounded-xl flex items-center justify-center shadow-md group-hover:shadow-lg transition-all duration-200 flex-shrink-0`}>
                  <Icon className="w-6 h-6 sm:w-7 h-7 text-white" />
                </div>
              </div>
            </div>
          );
        })}
      </div>

      {/* Quick Actions */}
      <div className="content-card">
        <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-6">
          <h3 className="text-subheading font-semibold">Quick Actions</h3>
        </div>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6">
          <button className="p-6 sm:p-8 border-2 border-dashed border-gray-300 rounded-xl sm:rounded-2xl hover:border-indigo-500 hover:bg-indigo-50 transition-all duration-300 group hover-lift">
            <div className="flex flex-col items-center gap-3 sm:gap-4 text-center">
              <div className="w-12 h-12 sm:w-16 sm:h-16 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-xl sm:rounded-2xl flex items-center justify-center shadow-lg group-hover:shadow-xl group-hover:scale-110 transition-all duration-300">
                <Sparkles className="w-6 h-6 sm:w-8 h-8 text-white" />
              </div>
              <div>
                <span className="font-semibold text-base sm:text-lg text-gray-900">Generate New Journal</span>
                <p className="text-xs sm:text-sm text-gray-600 mt-1 sm:mt-2">Start with AI-powered content</p>
              </div>
            </div>
          </button>

          <button className="p-6 sm:p-8 border-2 border-dashed border-gray-300 rounded-xl sm:rounded-2xl hover:border-indigo-500 hover:bg-indigo-50 transition-all duration-300 group hover-lift">
            <div className="flex flex-col items-center gap-3 sm:gap-4 text-center">
              <div className="w-12 h-12 sm:w-16 sm:h-16 bg-gradient-to-br from-blue-500 to-cyan-600 rounded-xl sm:rounded-2xl flex items-center justify-center shadow-lg group-hover:shadow-xl group-hover:scale-110 transition-all duration-300">
                <BookOpen className="w-6 h-6 sm:w-8 h-8 text-white" />
              </div>
              <div>
                <span className="font-semibold text-base sm:text-lg text-gray-900">Browse Templates</span>
                <p className="text-xs sm:text-sm text-gray-600 mt-1 sm:mt-2">Choose from pre-built themes</p>
              </div>
            </div>
          </button>

          <button className="p-6 sm:p-8 border-2 border-dashed border-gray-300 rounded-xl sm:rounded-2xl hover:border-indigo-500 hover:bg-indigo-50 transition-all duration-300 group hover-lift">
            <div className="flex flex-col items-center gap-3 sm:gap-4 text-center">
              <div className="w-12 h-12 sm:w-16 sm:h-16 bg-gradient-to-br from-green-500 to-emerald-600 rounded-xl sm:rounded-2xl flex items-center justify-center shadow-lg group-hover:shadow-xl group-hover:scale-110 transition-all duration-300">
                <TrendingUp className="w-6 h-6 sm:w-8 h-8 text-white" />
              </div>
              <div>
                <span className="font-semibold text-base sm:text-lg text-gray-900">View Analytics</span>
                <p className="text-xs sm:text-sm text-gray-600 mt-1 sm:mt-2">Track your progress</p>
              </div>
            </div>
          </button>
        </div>
      </div>

      {/* Recent Projects */}
      <div className="content-card">
        <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-6 sm:mb-8">
          <h3 className="text-subheading font-semibold">Recent Projects</h3>
          <button className="btn btn-outline flex items-center gap-2">
            <span>View All</span>
            <ArrowRight className="w-4 h-4" />
          </button>
        </div>

        {recentProjects.length === 0 ? (
          <div className="text-center py-12 sm:py-16">
            <div className="w-16 h-16 sm:w-20 sm:h-20 bg-gradient-to-br from-gray-100 to-gray-200 rounded-xl sm:rounded-2xl flex items-center justify-center mx-auto mb-4 sm:mb-6">
              <FileText className="w-8 h-8 sm:w-10 h-10 text-gray-400" />
            </div>
            <h4 className="text-lg sm:text-xl font-semibold text-gray-900 mb-2 sm:mb-3">No projects yet</h4>
            <p className="text-sm sm:text-base text-gray-600 mb-6 sm:mb-8 max-w-md mx-auto px-4">Create your first AI-powered journal to get started with personalized content generation</p>
            <button
              onClick={() => handleCreateJournal()}
              className="btn btn-primary flex items-center gap-2 mx-auto text-base sm:text-lg px-6 sm:px-8 py-3 shadow-lg hover:shadow-xl hover-lift"
            >
              <Plus className="w-4 h-4 sm:w-5 h-5" />
              Create New Journal
            </button>
          </div>
        ) : (
          <div className="space-y-4 sm:space-y-6">
            {recentProjects.map((project) => (
              <div key={project.id} className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 p-4 sm:p-6 bg-gradient-to-br from-gray-50 to-white rounded-xl sm:rounded-2xl border border-gray-100 hover:shadow-lg hover:border-indigo-200 transition-all duration-300 group">
                <div className="flex items-start gap-3 sm:gap-4 md:gap-6">
                  <div className="w-12 h-12 sm:w-14 sm:h-14 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-xl sm:rounded-2xl flex items-center justify-center shadow-md group-hover:shadow-lg transition-all duration-300 flex-shrink-0">
                    <FileText className="w-6 h-6 sm:w-7 h-7 text-white" />
                  </div>
                  <div className="flex-1 min-w-0">
                    <h4 className="font-semibold text-base sm:text-lg text-gray-900 mb-1 sm:mb-2 truncate">{project.title}</h4>
                    <p className="text-sm sm:text-base text-gray-600 mb-2 sm:mb-3 line-clamp-2">{project.description}</p>
                    <div className="flex flex-wrap items-center gap-3 sm:gap-4 md:gap-6 text-xs sm:text-sm text-gray-500">
                      <span className="flex items-center gap-1">
                        <FileText className="w-3 h-3 sm:w-4 h-4" />
                        {project.wordCount} words
                      </span>
                      <span className="flex items-center gap-1">
                        <Clock className="w-3 h-3 sm:w-4 h-4" />
                        {project.lastEdit}
                      </span>
                      {project.files && project.files.length > 0 && (
                        <span className="flex items-center gap-1">
                          <Users className="w-3 h-3 sm:w-4 h-4" />
                          {project.files.length} file{project.files.length > 1 ? 's' : ''}
                        </span>
                      )}
                    </div>
                    {project.files && project.files.length > 0 && (
                      <div className="mt-2 sm:mt-3 flex flex-wrap gap-1.5 sm:gap-2">
                        {project.files.slice(0, 3).map((file, index) => (
                          <span key={index} className="inline-flex items-center px-2 py-1 sm:px-3 sm:py-1.5 rounded-full text-xs bg-indigo-100 text-indigo-800 font-medium">
                            <FileText className="w-3 h-3 mr-1" />
                            <span className="truncate max-w-20 sm:max-w-none">{file.name}</span>
                            <span className="ml-1 text-indigo-600 font-semibold">({Math.round(file.size / 1024)}KB)</span>
                          </span>
                        ))}
                        {project.files.length > 3 && (
                          <span className="inline-flex items-center px-2 py-1 sm:px-3 sm:py-1.5 rounded-full text-xs bg-gray-100 text-gray-800 font-medium">
                            +{project.files.length - 3} more
                          </span>
                        )}
                      </div>
                    )}
                  </div>
                </div>

                <div className="flex sm:flex-col items-center gap-3 sm:gap-2">
                  <div className="text-center sm:text-right">
                    <div className="text-base sm:text-lg font-bold text-gray-900">{project.progress}%</div>
                    <div className="text-xs sm:text-sm font-medium text-gray-600 capitalize">{project.status}</div>
                  </div>
                  <div className="w-24 sm:w-32 bg-gray-200 rounded-full h-2 sm:h-3">
                    <div
                      className="bg-gradient-to-r from-indigo-500 to-purple-600 h-2 sm:h-3 rounded-full transition-all duration-500 shadow-sm"
                      style={{ width: `${project.progress}%` }}
                    />
                  </div>
                </div>
              </div>
            ))}
          </div>
        )}
      </div>

      {/* Activity Section */}
      <div className="content-card">
        <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-6 sm:mb-8">
          <h3 className="text-subheading font-semibold flex items-center gap-3">
            <div className="w-8 h-8 sm:w-10 sm:h-10 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-xl flex items-center justify-center">
              <Activity className="w-4 h-4 sm:w-5 h-5 text-white" />
            </div>
            Recent Activity
          </h3>
          <button className="btn btn-ghost text-sm">
            View All Activity
          </button>
        </div>

        {recentProjects.length === 0 ? (
          <div className="text-center py-8 sm:py-12">
            <div className="w-12 h-12 sm:w-16 sm:h-16 bg-gray-100 rounded-xl sm:rounded-2xl flex items-center justify-center mx-auto mb-3 sm:mb-4">
              <Activity className="w-6 h-6 sm:w-8 h-8 text-gray-400" />
            </div>
            <h4 className="text-base sm:text-lg font-medium text-gray-900 mb-1 sm:mb-2">No recent activity</h4>
            <p className="text-sm sm:text-base text-gray-600 px-4">Start creating journals to see your activity history here</p>
          </div>
        ) : (
          <div className="space-y-3 sm:space-y-6">
            <div className="flex items-start gap-3 sm:gap-4 p-3 sm:p-4 bg-gradient-to-r from-green-50 to-transparent rounded-lg sm:rounded-xl border-l-4 border-green-500">
              <div className="w-2.5 h-2.5 sm:w-3 sm:h-3 bg-green-500 rounded-full mt-1 sm:mt-1.5 shadow-sm flex-shrink-0"></div>
              <div className="flex-1 min-w-0">
                <p className="font-medium text-sm sm:text-base text-gray-900">Journal generation completed</p>
                <p className="text-xs sm:text-sm text-gray-600 mt-0.5 sm:mt-1">Your AI-powered journal was successfully created with personalized content</p>
                <p className="text-xs text-gray-500 mt-1 sm:mt-2">2 hours ago</p>
              </div>
            </div>

            <div className="flex items-start gap-3 sm:gap-4 p-3 sm:p-4 bg-gradient-to-r from-blue-50 to-transparent rounded-lg sm:rounded-xl border-l-4 border-blue-500">
              <div className="w-2.5 h-2.5 sm:w-3 sm:h-3 bg-blue-500 rounded-full mt-1 sm:mt-1.5 shadow-sm flex-shrink-0"></div>
              <div className="flex-1 min-w-0">
                <p className="font-medium text-sm sm:text-base text-gray-900">API key configured</p>
                <p className="text-xs sm:text-sm text-gray-600 mt-0.5 sm:mt-1">OpenAI API key successfully added to your account settings</p>
                <p className="text-xs text-gray-500 mt-1 sm:mt-2">3 days ago</p>
              </div>
            </div>

            <div className="flex items-start gap-3 sm:gap-4 p-3 sm:p-4 bg-gradient-to-r from-purple-50 to-transparent rounded-lg sm:rounded-xl border-l-4 border-purple-500">
              <div className="w-2.5 h-2.5 sm:w-3 sm:h-3 bg-purple-500 rounded-full mt-1 sm:mt-1.5 shadow-sm flex-shrink-0"></div>
              <div className="flex-1 min-w-0">
                <p className="font-medium text-sm sm:text-base text-gray-900">Account created</p>
                <p className="text-xs sm:text-sm text-gray-600 mt-0.5 sm:mt-1">Welcome to Journal Craft Crew! Your account is ready to create amazing journals</p>
                <p className="text-xs text-gray-500 mt-1 sm:mt-2">1 week ago</p>
              </div>
            </div>
          </div>
        )}
        </div>
      </div>
    </div>
  )
};

export default Dashboard;